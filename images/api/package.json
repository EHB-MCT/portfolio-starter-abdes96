{
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "knex": "^3.1.0",
    "nodemon": "^3.0.2",
    "pg": "^8.11.3",
    "three": "^0.160.0"
  },
  "name": "api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test-watch": "jest --watchAll --detectOpenHandles",
    "test": "jest --detectOpenHandles",
    "start": "knex migrate:latest && nodemon -L src/index.js "
    "start": "knex migrate:latest && knex seed:run && nodemon src/index.js && npm run test",
    "test": "jest --detectOpenHandles --forceExit",
    "test-watch": "jest --detectOpenHandles --watchAll"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "assert": "^2.1.0",
    "jest": "^29.7.0",
    "jest-cli": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
